# 📦 Ручной деплой АСИТ Дейли на Jino.ru

## ✅ Архив готов!

Файл: `asit-daily-deploy-20260208-174500.tar.gz` (340 KB)

Этот архив содержит:
- ✅ Frontend (public_html/)
- ✅ Backend (backend/dist/)
- ✅ .env с ВАШИМИ данными (MySQL, Email, секреты)
- ✅ app.js (entry point для Passenger)

---

## 🚀 Способ 1: Загрузка через File Manager (САМЫЙ ПРОСТОЙ)

### Шаг 1: Откройте File Manager в панели Jino

1. Откройте: https://jino.ru/panel
2. Управление → **File Manager**
3. Перейдите в `/domains/asitdaily.ru/`

### Шаг 2: Загрузите архив

1. Нажмите кнопку **Upload** (Загрузить)
2. Выберите файл `asit-daily-deploy-20260208-174500.tar.gz`
3. Дождитесь завершения загрузки

### Шаг 3: Распакуйте архив

1. Найдите загруженный файл в File Manager
2. Правой кнопкой → **Extract** (Распаковать)
3. Выберите путь: `/domains/asitdaily.ru/`
4. Нажмите OK

### Шаг 4: Удалите архив (опционально)

1. Выберите `asit-daily-deploy-20260208-174500.tar.gz`
2. Удалить

### Шаг 5: Создайте папку tmp

1. В `/domains/asitdaily.ru/` нажмите **New Folder**
2. Имя: `tmp`
3. В папке `tmp` создайте пустой файл `restart.txt`

### Шаг 6: Установите зависимости backend

**В Terminal (через панель Jino или SSH):**

```bash
cd /domains/asitdaily.ru/backend
npm install --production
```

### Шаг 7: Настройте интерпретатор Node.js

1. В панели Jino: Управление → **Домены** → **asitdaily.ru** → **Настройки**
2. Интерпретатор: **Node.js** (выберите последнюю версию)
3. Сохранить

### Шаг 8: Перезапустите приложение

```bash
touch /domains/asitdaily.ru/tmp/restart.txt
```

### Шаг 9: Проверьте работу

Подождите 30 секунд, затем:

```bash
curl https://asitdaily.ru/health
```

Должно вернуть: `{"status":"ok",...}`

Откройте в браузере: **https://asitdaily.ru**

---

## 🚀 Способ 2: Загрузка через SFTP (FileZilla)

### Шаг 1: Установите FileZilla

Скачайте: https://filezilla-project.org/

### Шаг 2: Подключитесь к серверу

В FileZilla:
- **Host:** `sftp://asitdaily.ru` или `sftp://roller25.myjino.ru`
- **Username:** `roller25`
- **Password:** (ваш пароль от панели Jino)
- **Port:** `22`

Нажмите **Quickconnect**

### Шаг 3: Загрузите архив

1. В левой панели (локальный компьютер) перейдите в:
   `/Users/kotovod/Documents/GitHub/ASIT Daily/`
2. В правой панели (сервер) перейдите в:
   `/domains/asitdaily.ru/`
3. Перетащите файл `asit-daily-deploy-20260208-174500.tar.gz` с левой панели на правую

### Шаг 4: Распакуйте через Terminal

Если у вас есть SSH доступ (или используйте Terminal в панели Jino):

```bash
cd /domains/asitdaily.ru
tar -xzf asit-daily-deploy-20260208-174500.tar.gz
rm asit-daily-deploy-20260208-174500.tar.gz
```

Затем следуйте шагам 5-9 из **Способа 1**.

---

## 🚀 Способ 3: Через SSH (если настроите ключ)

### Настройка SSH ключа:

```bash
# 1. Создайте SSH ключ (если его нет)
ssh-keygen -t rsa -b 4096 -C "roller25@asitdaily.ru"
# Нажмите Enter 3 раза

# 2. Скопируйте публичный ключ
cat ~/.ssh/id_rsa.pub

# 3. Добавьте ключ в панель Jino:
# https://jino.ru/panel → Управление → SSH-доступ
```

### После настройки ключа:

```bash
# Загрузите архив
scp asit-daily-deploy-20260208-174500.tar.gz roller25@asitdaily.ru:~/

# Подключитесь к серверу
ssh roller25@asitdaily.ru

# Распакуйте
cd /domains/asitdaily.ru
tar -xzf ~/asit-daily-deploy-20260208-174500.tar.gz
rm ~/asit-daily-deploy-20260208-174500.tar.gz

# Установите зависимости
cd backend
npm install --production

# Создайте tmp
cd ..
mkdir -p tmp
touch tmp/restart.txt
```

---

## 📂 Итоговая структура на сервере

```
/domains/asitdaily.ru/
├── app.js                 ← Entry point (Passenger)
├── backend/
│   ├── dist/              ← Backend compiled
│   ├── node_modules/      ← После npm install
│   ├── .env               ← УЖЕ ВКЛЮЧЁН С ВАШИМИ ДАННЫМИ!
│   └── package.json
├── public_html/           ← Frontend
│   ├── index.html
│   ├── assets/
│   ├── sw.js
│   └── manifest.webmanifest
└── tmp/
    └── restart.txt        ← Для перезапуска
```

---

## ✅ Проверка после деплоя

```bash
# Health check
curl https://asitdaily.ru/health

# Логи
tail -f /domains/asitdaily.ru/logs/passenger.log

# Перезапуск
touch /domains/asitdaily.ru/tmp/restart.txt
```

---

## 🆘 Если что-то не работает

### Проверьте логи:

```bash
tail -f /domains/asitdaily.ru/logs/passenger.log
```

### Распространённые проблемы:

1. **502 Bad Gateway** → Проверьте, что Node.js выбран в настройках домена
2. **Module not found** → Запустите `npm install` в папке backend
3. **Database error** → Проверьте .env файл (DB_NAME, DB_USER, DB_PASSWORD)
4. **Permission denied** → Проверьте права: `chmod +x app.js`

---

## 💡 Важно!

- ✅ .env файл УЖЕ содержит ваши данные MySQL и Email
- ✅ Все секреты (JWT, VAPID) уже настроены
- ✅ После первого запуска база автоматически создаст таблицы
- ✅ SSL уже настроен Jino автоматически

---

**Готово! После выполнения этих шагов ваше приложение будет работать на https://asitdaily.ru** 🎉
