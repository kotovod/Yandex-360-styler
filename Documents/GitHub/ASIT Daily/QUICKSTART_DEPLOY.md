# 🚀 Быстрый старт: Деплой АСИТ Дейли

Пошаговая инструкция для деплоя приложения на production.

---

## ⏱️ Время на деплой: ~30 минут

---

## 📋 Что вам понадобится

- [ ] Аккаунт GitHub (бесплатно)
- [ ] Аккаунт Vercel (бесплатно)
- [ ] Аккаунт Railway (бесплатно, $5 в месяц после trial)
- [ ] MySQL база на Jino.ru (у вас уже есть)
- [ ] Email на Jino.ru с включённым SMTP (у вас уже есть)

---

## 🎯 Шаг 1: Подготовка кода (5 минут)

### 1.1 Создайте репозиторий на GitHub

```bash
# Если ещё не создан git репозиторий
cd "/Users/kotovod/Documents/GitHub/ASIT Daily"
git init
git add .
git commit -m "Initial commit: ASIT Daily v1.0.0"

# Создайте репозиторий на github.com и добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/asit-daily.git
git branch -M main
git push -u origin main
```

### 1.2 Создайте .gitignore (если нет)

Убедитесь, что файл `.gitignore` содержит:
```
node_modules/
dist/
.env
.env.local
.DS_Store
backend/data/*.db
```

---

## 🗄️ Шаг 2: Настройка MySQL на Jino (10 минут)

### 2.1 Создайте базу данных

1. Зайдите на https://jino.ru → Панель управления
2. Раздел "Базы данных" → "MySQL"
3. Нажмите "Создать базу данных":
   - **Имя**: `asit_daily`
   - **Кодировка**: `utf8mb4_unicode_ci`
4. Создайте пользователя:
   - **Логин**: `asit_user`
   - **Пароль**: (сгенерируйте сложный, сохраните)
5. Назначьте пользователю все права на базу `asit_daily`

### 2.2 Включите удалённый доступ

1. В настройках MySQL найдите "Удалённый доступ"
2. Включите опцию "Разрешить удалённый доступ"
3. Если возможно, укажите IP Railway (узнаете позже)
4. Или разрешите доступ с любого IP: `%` (менее безопасно)

### 2.3 Сохраните данные подключения

```
DB_HOST=mysql.jino.ru (или ваш хост)
DB_PORT=3306
DB_NAME=asit_daily
DB_USER=asit_user
DB_PASSWORD=ваш-пароль
```

---

## 🚂 Шаг 3: Деплой Backend на Railway (10 минут)

### 3.1 Создайте проект

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите ваш репозиторий `asit-daily`
5. Railway автоматически обнаружит backend

### 3.2 Настройте переменные окружения

В Railway Dashboard → ваш проект → Variables, добавьте:

```bash
# Node
NODE_ENV=production
PORT=3000

# Database (MySQL Jino)
DB_HOST=mysql.jino.ru
DB_PORT=3306
DB_NAME=asit_daily
DB_USER=asit_user
DB_PASSWORD=ваш-пароль-от-mysql
DB_DIALECT=mysql

# JWT (сгенерируйте ниже)
JWT_SECRET=ваш-секретный-ключ-64-символа

# Email (Jino SMTP)
EMAIL_HOST=smtp.jino.ru
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=noreply@asitdaily.ru
EMAIL_PASSWORD=ваш-пароль-от-почты

# VAPID для push-уведомлений (сгенерируйте ниже)
VAPID_PUBLIC_KEY=ваш-публичный-ключ
VAPID_PRIVATE_KEY=ваш-приватный-ключ
VAPID_EMAIL=mailto:noreply@asitdaily.ru
```

### 3.3 Генерация секретных ключей

#### JWT Secret (на вашем компьютере):
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```
Скопируйте результат в `JWT_SECRET`

#### VAPID Keys:
```bash
npx web-push generate-vapid-keys
```
Скопируйте:
- Public Key → `VAPID_PUBLIC_KEY`
- Private Key → `VAPID_PRIVATE_KEY`

### 3.4 Настройте root directory

В Railway Dashboard → Settings → Root Directory:
```
backend
```

### 3.5 Деплой

1. Railway автоматически задеплоит после настройки
2. Дождитесь завершения (3-5 минут)
3. Скопируйте URL вашего backend: `https://asit-daily-backend-production.up.railway.app`

### 3.6 Проверьте работу

```bash
curl https://ваш-backend-url.railway.app/health
```

Должны увидеть:
```json
{
  "status": "ok",
  "database": "MySQL",
  "message": "✅ Backend полностью работает!"
}
```

---

## 🌐 Шаг 4: Деплой Frontend на Vercel (5 минут)

### 4.1 Создайте проект

1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите "Add New" → "Project"
4. Импортируйте ваш репозиторий `asit-daily`

### 4.2 Настройте проект

- **Framework Preset**: Vite
- **Root Directory**: `.` (корень)
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

### 4.3 Добавьте переменную окружения

В настройках проекта → Environment Variables:

```
VITE_API_URL=https://ваш-backend-url.railway.app/api
```

⚠️ **Важно**: Замените на реальный URL вашего Railway backend!

### 4.4 Деплой

1. Нажмите "Deploy"
2. Дождитесь завершения (2-3 минуты)
3. Vercel даст вам URL: `https://asit-daily.vercel.app`

---

## ✅ Шаг 5: Тестирование (5 минут)

### 5.1 Откройте приложение

Перейдите на ваш Vercel URL: `https://asit-daily.vercel.app`

### 5.2 Протестируйте регистрацию

1. Нажмите "Регистрация"
2. Введите реальный email
3. Проверьте получение кода на почту
4. Введите код и войдите

### 5.3 Протестируйте основные функции

- [ ] Начать терапию
- [ ] Отметить приём дозы
- [ ] Посмотреть историю
- [ ] Посмотреть календарь
- [ ] Экспортировать PDF
- [ ] Выйти и войти снова

---

## 🎉 Готово!

Ваше приложение задеплоено и работает!

### Ваши URL:
- **Frontend**: `https://asit-daily.vercel.app`
- **Backend**: `https://ваш-backend.railway.app`

### Автоматический деплой

Теперь при каждом push в GitHub:
- ✅ Vercel автоматически обновит frontend
- ✅ Railway автоматически обновит backend

---

## 🔧 Настройка домена (опционально)

### Подключение asitdaily.ru к Vercel

1. В Vercel Dashboard → Settings → Domains
2. Добавьте домен: `asitdaily.ru` и `www.asitdaily.ru`
3. Vercel покажет DNS записи для настройки
4. В панели Jino.ru → Домены → DNS:
   - Добавьте A-запись: `@` → IP от Vercel
   - Добавьте CNAME: `www` → `cname.vercel-dns.com`
5. Дождитесь пропагации DNS (до 24 часов)
6. SSL сертификат установится автоматически

---

## 🆘 Проблемы?

### Backend не запускается
```bash
# Проверьте логи в Railway
# Dashboard → Deployments → View Logs
```

### Frontend не подключается к Backend
```bash
# Проверьте переменную VITE_API_URL в Vercel
# Она должна указывать на Railway URL
```

### Email не отправляется
```bash
# Проверьте в Jino панели:
# "Разрешить доступ к SMTP" должно быть включено
```

### База данных не подключается
```bash
# Проверьте "Удалённый доступ" в MySQL Jino
# Попробуйте подключиться с локального компьютера:
mysql -h mysql.jino.ru -u asit_user -p asit_daily
```

---

## 📞 Дополнительная помощь

- Railway Docs: https://docs.railway.app
- Vercel Docs: https://vercel.com/docs
- Jino Help: https://jino.ru/help

---

**Следующий шаг**: [DEPLOYMENT_JINO.md](./DEPLOYMENT_JINO.md) - полная документация по деплою
