# Деплой АСИТ Дейли на Jino.ru и облачные сервисы

## 🎯 Архитектура деплоя

```
Frontend (Vercel/Netlify)  →  Backend (Railway/Render)  →  MySQL (Jino.ru)
         ↓                            ↓                         ↓
    Static files              Node.js API               Database
    HTTPS автоматически       HTTPS автоматически       Удалённый доступ
```

---

## 📦 Версионирование

### Система версий: Semantic Versioning (semver)

```
MAJOR.MINOR.PATCH
  ↓     ↓     ↓
  1  .  0  .  0

MAJOR - Несовместимые изменения API
MINOR - Новые функции, обратная совместимость
PATCH - Исправления багов
```

### Текущая версия
- **Версия**: `1.0.0`
- **Дата**: 8 февраля 2026
- **Статус**: Development

---

## 🚀 Часть 1: Деплой Frontend на Vercel

### Шаг 1: Установка и настройка

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login
```

### Шаг 2: Настройка переменных окружения

Создайте файл `.env.production` в корне проекта:

```bash
VITE_API_URL=https://your-backend.railway.app/api
```

### Шаг 3: Деплой

```bash
# Первый деплой (из корня проекта)
vercel

# Production деплой
vercel --prod
```

### Шаг 4: Настройка в Vercel Dashboard

1. Зайдите на https://vercel.com/dashboard
2. Выберите ваш проект
3. Settings → Environment Variables
4. Добавьте:
   - `VITE_API_URL` = URL вашего backend

### Автоматический деплой из GitHub

1. Подключите GitHub репозиторий к Vercel
2. Каждый push в `main` → автоматический деплой
3. Pull Request → preview деплой

---

## 🔧 Часть 2: Деплой Backend на Railway

### Шаг 1: Подготовка проекта

```bash
cd backend
```

### Шаг 2: Создайте `Procfile` (если нужно)

```
web: npm start
```

### Шаг 3: Установка Railway CLI

```bash
npm i -g @railway/cli
```

### Шаг 4: Деплой

```bash
# Вход в Railway
railway login

# Инициализация проекта
railway init

# Деплой
railway up
```

### Шаг 5: Настройка переменных окружения в Railway

В Railway Dashboard добавьте:

```bash
# Основные
NODE_ENV=production
PORT=3000

# База данных (MySQL на Jino)
DB_HOST=mysql.jino.ru
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_DIALECT=mysql

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this

# Email (Jino SMTP)
EMAIL_HOST=smtp.jino.ru
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=noreply@asitdaily.ru
EMAIL_PASSWORD=your-email-password

# VAPID (для push-уведомлений)
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_EMAIL=mailto:noreply@asitdaily.ru
```

### Генерация секретных ключей

```bash
# JWT Secret
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# VAPID Keys
npx web-push generate-vapid-keys
```

---

## 🗄️ Часть 3: Настройка MySQL на Jino

### Шаг 1: Создание базы данных

1. Зайдите в панель Jino.ru
2. Раздел "Базы данных" → MySQL
3. Создайте новую базу данных:
   - Имя: `asit_daily`
   - Пользователь: создайте нового
   - Пароль: сохраните надёжный пароль

### Шаг 2: Включение удалённого доступа

1. В настройках MySQL найдите "Удалённый доступ"
2. Разрешите доступ с IP адреса Railway
   - Или разрешите с любого IP (менее безопасно)

### Шаг 3: Обновление backend для MySQL

Backend уже настроен на работу с MySQL через Sequelize.
Убедитесь, что в `backend/src/config/index.ts` используется правильный диалект:

```typescript
// В production использовать MySQL
const dbDialect = process.env.NODE_ENV === 'production' ? 'mysql' : 'sqlite';
```

---

## 🔄 Процесс релиза новой версии

### 1. Обновление версии

```bash
# В корне проекта
npm version patch  # 1.0.0 → 1.0.1 (багфиксы)
npm version minor  # 1.0.0 → 1.1.0 (новые функции)
npm version major  # 1.0.0 → 2.0.0 (breaking changes)

# В backend
cd backend
npm version patch
```

### 2. Создание changelog

Обновите `CHANGELOG.md`:

```markdown
## [1.1.0] - 2026-02-08

### Добавлено
- Email верификация при регистрации
- Восстановление пароля по email
- Проверка существования email перед отправкой кода

### Исправлено
- Автоматический вход без подтверждения email

### Изменено
- Название приложения на "АСИТ Дейли"
```

### 3. Коммит и тег

```bash
# Коммит изменений
git add .
git commit -m "chore: release v1.1.0"

# Создание тега
git tag -a v1.1.0 -m "Release v1.1.0: Email verification and password reset"

# Push в GitHub
git push origin main --tags
```

### 4. Автоматический деплой

При push с тегом:
- Vercel автоматически деплоит frontend
- Railway автоматически деплоит backend

---

## 📋 Чек-лист перед production деплоем

### Frontend
- [ ] `VITE_API_URL` указывает на production backend
- [ ] Service Worker настроен на HTTPS
- [ ] PWA manifest содержит правильные иконки
- [ ] Все изображения оптимизированы

### Backend
- [ ] Все переменные окружения настроены
- [ ] JWT_SECRET уникальный и сложный
- [ ] CORS настроен на production домен
- [ ] MySQL база данных создана и доступна
- [ ] Email SMTP работает (тест регистрации)
- [ ] VAPID ключи сгенерированы

### База данных
- [ ] Создана на Jino
- [ ] Удалённый доступ включен
- [ ] Пользователь с правильными правами
- [ ] Резервное копирование настроено

### Безопасность
- [ ] Все секреты в переменных окружения, не в коде
- [ ] `.env` в `.gitignore`
- [ ] HTTPS на всех сервисах
- [ ] Rate limiting на критичных endpoints
- [ ] SQL injection защита (Sequelize ORM)

---

## 🧪 Тестирование production

### 1. Health Check

```bash
curl https://your-backend.railway.app/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "2026-02-08T...",
  "database": "MySQL",
  "message": "✅ Backend полностью работает!"
}
```

### 2. Регистрация пользователя

1. Откройте https://your-app.vercel.app
2. Зарегистрируйтесь с реальным email
3. Проверьте получение кода на почту
4. Подтвердите email
5. Войдите в систему

### 3. Функциональное тестирование

- [ ] Начать терапию
- [ ] Отметить приём дозы
- [ ] Добавить побочный эффект
- [ ] Экспортировать данные в PDF
- [ ] Выйти и войти снова

---

## 🔍 Мониторинг и логи

### Railway
- Dashboard → Deployments → Logs
- Мониторинг CPU/Memory
- Статус деплоя

### Vercel
- Dashboard → Deployments
- Analytics
- Функции и производительность

### База данных
- Jino панель → MySQL → Мониторинг
- Размер базы
- Количество запросов

---

## 🆘 Troubleshooting

### Frontend не подключается к Backend

```bash
# Проверьте переменную окружения
echo $VITE_API_URL

# Проверьте backend
curl https://your-backend/health

# Проверьте CORS в backend/src/index.ts
```

### Database connection error

```bash
# Проверьте доступность MySQL
mysql -h mysql.jino.ru -u your_user -p

# Проверьте переменные в Railway
DB_HOST, DB_NAME, DB_USER, DB_PASSWORD
```

### Email не отправляется

```bash
# Проверьте SMTP в Jino панели
# Убедитесь, что "Разрешить доступ к SMTP" включено

# Проверьте логи в Railway
railway logs
```

---

## 📊 Git Flow для релизов

```
main (production)
  ├── develop (staging)
  │   ├── feature/email-verification
  │   ├── feature/password-reset
  │   └── bugfix/dose-calculation

Release process:
1. feature → develop (pull request)
2. develop → main (release pull request + tag)
3. main → auto-deploy to production
```

---

## 🎉 Готово!

После деплоя ваше приложение будет доступно:
- **Frontend**: https://asit-daily.vercel.app
- **Backend**: https://asit-daily-backend.railway.app
- **Database**: MySQL на Jino.ru

Следующие шаги:
1. Настройте custom домен (asitdaily.ru)
2. Настройте SSL сертификаты (автоматически)
3. Мониторинг и аналитика
4. Резервное копирование базы данных
