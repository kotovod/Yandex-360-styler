# 📧 Настройка отправки email

Приложение использует email для:
- ✅ Подтверждения регистрации (6-значный код)
- 🔐 Восстановления пароля (6-значный код)

## Быстрая настройка с Gmail

### 1. Создайте пароль приложения Gmail

1. Перейдите на https://myaccount.google.com/security
2. Включите **"Двухэтапная аутентификация"** (если еще не включена)
3. После включения 2FA, перейдите в **"Пароли приложений"**
4. Выберите "Почта" и "Другое устройство"
5. Скопируйте сгенерированный пароль (16 символов без пробелов)

### 2. Настройте .env файл

Создайте файл `.env` в папке `backend/`:

```env
# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-16-char-app-password
```

### 3. Перезапустите backend

```bash
cd backend
npm run dev
```

## Использование других email провайдеров

### Mail.ru

```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=your-email@mail.ru
EMAIL_PASSWORD=your-password
```

### Yandex

```env
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USER=your-email@yandex.ru
EMAIL_PASSWORD=your-password
```

### Настраиваемый SMTP

```env
EMAIL_HOST=smtp.your-provider.com
EMAIL_PORT=587
EMAIL_USER=your-email@your-domain.com
EMAIL_PASSWORD=your-password
```

## Тестирование

После настройки:

1. Зарегистрируйте нового пользователя
2. Проверьте почту - должно прийти письмо с 6-значным кодом
3. Введите код для подтверждения email
4. Попробуйте "Забыли пароль?" для проверки восстановления

## Важные замечания

⚠️ **Безопасность:**
- Никогда не коммитьте `.env` файл в git (он уже в `.gitignore`)
- Используйте пароли приложений, не основной пароль
- Для production используйте переменные окружения сервера

⚠️ **Gmail лимиты:**
- 500 писем в день для бесплатных аккаунтов
- Может потребоваться разрешить "менее безопасные приложения"

⚠️ **Если письма не приходят:**
- Проверьте папку "Спам"
- Убедитесь, что EMAIL_USER и EMAIL_PASSWORD правильные
- Проверьте логи backend на наличие ошибок
- Попробуйте другой SMTP сервер

## Отключение email (для разработки)

Если вы хотите протестировать без email:

1. Оставьте EMAIL_USER пустым в `.env`
2. Backend не будет падать, просто письма не будут отправляться
3. Смотрите код в логах backend (он там выводится для отладки)

## Поддержка

Если возникли проблемы с настройкой email:
- Проверьте логи backend (`npm run dev`)
- Убедитесь, что порты 587 или 465 не заблокированы
- Попробуйте другой email провайдер
